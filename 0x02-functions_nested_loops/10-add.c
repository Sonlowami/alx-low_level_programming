#include "main"

/**
 * add - adds two integers and return the result
 * @x: the first operand
 * @y: the second operand
 * Return: integer summary of x and y
 */

int add(int x, int y)
{
	return (x + y);
}
